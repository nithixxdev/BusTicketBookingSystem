CREATE DATABASE IF NOT EXISTS bus_booking_db;
USE bus_booking_db;
-- The application creates tables automatically through JPA.
-- Start the application once after configuring application.properties.
-- Default admin is created automatically:
-- Email: admin@busgo.com
-- Password: Admin@123
